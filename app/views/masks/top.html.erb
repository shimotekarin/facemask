<h1>パックについて</h1>

<h2>人気投稿TOP5</h2>
<% @rank_masks.each do |t| %>
  <div class="mask">
  <% if t.user.present? %>
  <%= link_to t.user.name, user_path(t.user.id) %> 
   <% else %>
  <% end %>

    <% if t.image.attached? %>
      <%= image_tag t.image, size: '250x200'%>
    <% end %>
        
      <%= t.mask_name %>
      <%= t.skintype %>
      <%= t.body %>

    <div>
    <h4>総合評価：
    <span class="star-rating">
        <span class="star-rating-front" style="width: <%= getPercent(t.overall) %>%;">★★★★★</span>
        <span class="star-rating-back">★★★★★</span>
    </span> 
    </h4>
    </div>
    
    <%= l t.updated_at, format: :short %>

    <% if user_signed_in? %>
     <% if current_user.already_liked?(t) %>
        <%= link_to mask_likes_path(id: t.id, mask_id: t.id), data: { turbo_method: :delete } do %>
          <i class="far fa-heart"></i><%= t.likes.count %>
        <% end %>
     <% else %>
        <%= link_to mask_likes_path(id: t.id, mask_id: t.id), data: { turbo_method: :post }  do %>
           <i class="far fa-heart"></i><%= t.likes.count %>
        <% end %>
     <% end %>
    <% else %>
    <i class="far fa-heart"></i><%= t.likes.count %>
    <% end %>
<%= link_to "詳細", mask_path(t.id) %>
<% end %>